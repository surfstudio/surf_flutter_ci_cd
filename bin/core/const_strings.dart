/// Константные строки с названиями флагов.
class ConstStrings {
  /// Флаг используемого окружения.
  static const flagEnvironment = 'env';

  /// Флаг указания используемого проекта.
  static const flagProject = 'proj';

  /// Флаг используемого таргета
  static const flagTarget = 'target';

  /// Флаг указания метода выгрузки артефакта.
  static const flagDeploy = 'deploy-to';

  /// Сокращённое название флага используемого окружения.
  static const flagEnvironmentAbbr = 'env';

  /// Сокращённое название флага указания используемого проекта.
  static const flagProjectAbbr = 'proj';

  /// Сокращённое название флага используемого таргета
  static const flagTargetAbbr = 'target';

  /// Сокращённое название флага указания метода выгрузки артефакта.
  static const flagDeployAbbr = 'deploy-to';

  /// Подсказка флага используемого окружения.
  static const flagEnvironmentHint = 'Environment name';

  /// Подсказка флага указания используемого проекта.
  static const flagProjectHint = 'Project name';

  /// Подсказка флага используемого таргета
  static const flagTargetHint = 'Target platform';

  /// Подсказка флага указания метода выгрузки артефакта.
  static const flagDeployHint = 'Deploy to platform';
}
